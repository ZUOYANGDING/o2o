<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zuoyang.o2o.dao.ShopDao">
    <insert id="insertShop" useGeneratedKeys="true" keyColumn="shop_id" keyProperty="shopId">
        INSERT INTO
        tb_shop(shop_id, shop_name, shop_desc, shop_address, phone, shop_img, priority, enable_status, create_time,
        last_edit_time, advice, area_id, owner_id, shop_category_id)
        VALUES
        (#{shopId}, #{shopName}, #{shopDesc}, #{shopAddress}, #{phone}, #{shopImg}, #{priority}, #{enableStatus},
        #{createTime}, #{lastEditTime}, #{advice}, #{area.areaId}, #{personInfo.userId}, #{shopCategory.shopCategoryId})

    </insert>
</mapper>