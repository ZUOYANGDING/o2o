<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zuoyang.o2o.dao.ProductCategoryDao">
    <select id="queryProductCategoryList" resultType="zuoyang.o2o.entity.ProductCategory" parameterType="Long">
        SELECT product_category_id, product_category_name, priority, create_time, last_edit_time
        FROM tb_product_category
        WHERE shop_id = #{shopId}
        ORDER BY priority DESC
    </select>

    <insert id="batchInsertProductCategory" parameterType="java.util.List">
        INSERT INTO tb_product_category(product_category_name, priority, create_time, shop_id, last_edit_time)
        VALUES
        <foreach collection="list" item="productCategory" index="index" separator=",">
            (#{productCategory.productCategoryName}, #{productCategory.priority},
             #{productCategory.createTime}, #{productCategory.shopId},
             #{productCategory.lastEditTime})
        </foreach>
    </insert>

</mapper>